<Page
    x:Class="Portfolio.Pages.PortFolioHistoricalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Portfolio.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:Portfolio.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" Spacing="6">
        <controls:PeriodComboBox PeriodChanged="PeriodComboBox_PeriodChanged" HorizontalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
            <CheckBox IsChecked="{x:Bind ViewModel.IsAverageEnabled, Mode=TwoWay}" Content="Ajouter la moyenne" />
            <Slider Width="200" Minimum="5" Maximum="30" Value="{x:Bind ViewModel.AverageCount, Mode=TwoWay}" />
        </StackPanel>
        <controls:CandelierChart  Height="500" Values="{x:Bind ViewModel.Values, Mode=OneWay}" Stroke="CadetBlue" IsAverageEnabled="{x:Bind ViewModel.IsAverageEnabled, Mode=OneWay}" AverageCount="{x:Bind ViewModel.AverageCount, Mode=OneWay}"/>
    </StackPanel>
</Page>
