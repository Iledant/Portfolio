<Page
    x:Class="Portfolio.Pages.PortFolioEditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Portfolio.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" Padding="12" Spacing="6">
        <TextBlock 
            x:Name="Title" 
            Text="Créer une compagnie" 
            Style="{ThemeResource SubtitleTextBlockStyle}"
        />
        <TextBox Header="Nom" x:Name="NameBox" TextChanged="NameBox_TextChanged"/>
        <TextBox Header="Commentaire" x:Name="CommentBox" />
        <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
            <TextBlock 
                x:Name="AlreadyExistTextError" 
                Foreground="Red" 
                Text="Le portefeuille existe déjà" 
                Visibility="Collapsed"/>
            <Button 
                Content="Annuler"
                x:Name="CancelButton" 
                Click="PortFolioEditPageCancelButton_Clicked" />
            <Button 
                Content="Créer" 
                x:Name="OkButton" 
                Click="PortFolioEditPageOkButton_Clicked"/>
        </StackPanel>
    </StackPanel>
</Page>
