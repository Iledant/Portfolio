<ContentDialog
    x:Class="Portfolio.Dialogs.PortFolioLineEditDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Portfolio.Dialogs"
    xmlns:local1="using:Portfolio.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    IsPrimaryButtonEnabled="False"
    SecondaryButtonText="Annuler"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">
    <StackPanel Orientation="Vertical">
        <ComboBox 
            x:Name="FundComboBox" 
            Header="Nom du fond" 
            HorizontalAlignment="Stretch" 
            SelectionChanged="FundComboBox_SelectionChanged">
            <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="local1:Fund">
                    <TextBlock Text="{x:Bind Name}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBox Header="Quantité" x:Name="QuantityTextBox" TextChanged="QuantityTextBox_TextChanged" />
        <DatePicker x:Name="DatePicker" Header="Date d'achat" DateChanged="DatePicker_DateChanged"/>
        <TextBox  x:Name="AveragePriceTextBox" Header ="Prix d'achat" TextChanged="AveragePriceTextBox_TextChanged"/>
    </StackPanel>
</ContentDialog>
